function squadView() {
    let html = `<h1>Select your squad</h1>`;
 
        
        

    


    divApp.innerHTML = html;
}

    
/* 
if (model.characters[i].id == model.team[t].id) 
        html += `model.characters[i].name`; */